# Shipped Projects Visual Uplift Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the ASCII output samples on the Mega TTT and Walking/Jumping project cards with real visual assets, and add a prominent "play on itch.io" CTA to the Mega TTT card.

**Architecture:** Two new image files in `assets/projects/`, two card-level HTML updates, one new CSS rule, one new one-shot Python rendering script. No JS changes. No new dependencies — Pillow and matplotlib are already available locally.

**Tech Stack:** PIL/Pillow for the WebP encode, matplotlib for the confusion-matrix render, raw HTML edits to `index.html`, one rule appended to `styles.css`.

**Spec:** `docs/superpowers/specs/2026-05-12-shipped-projects-uplift.md`

**Working directory:** `C:/Users/14jak/GitHub/website/`

---

## File Structure

```
website/
├── assets/projects/
│   ├── mega-ttt.webp                NEW (~50 KB, from .tmp-mega/shot-3.png)
│   └── walking-jumping-cm.png       NEW (~25 KB, rendered)
├── scripts/
│   └── render-wj-cm.py              NEW (one-shot, kept for reproducibility)
├── index.html                       MODIFIED (Mega TTT card + Walking/Jumping card)
└── styles.css                       MODIFIED (one new rule for .project__cta)
```

`assets/projects/mega-ttt.webp` — the third screenshot from the itch.io
listing, already downloaded to `.tmp-mega/shot-3.png`. Convert and move.

`assets/projects/walking-jumping-cm.png` — generated by
`scripts/render-wj-cm.py`. Hard-coded confusion-matrix values match the
existing ASCII sample (182 / 7 / 5 / 156, accuracy 0.965, F1 0.964).

`scripts/render-wj-cm.py` — committed for reproducibility (so future
edits to the visualization can be done in source, not Photoshop).

`index.html` changes are scoped to the two card `<article>` blocks; no
restructuring elsewhere.

`styles.css` gets one new rule: `.project__cta { margin: 0.875rem 0 0; }`.

---

## Task 1: Convert the Mega TTT screenshot to WebP

**Files:**
- Source: `C:/Users/14jak/GitHub/website/.tmp-mega/shot-3.png` (already exists)
- Create: `C:/Users/14jak/GitHub/website/assets/projects/mega-ttt.webp`

- [ ] **Step 1: Run the conversion**

```bash
python -c "from PIL import Image; im = Image.open(r'C:/Users/14jak/GitHub/website/.tmp-mega/shot-3.png'); im.save(r'C:/Users/14jak/GitHub/website/assets/projects/mega-ttt.webp', 'WEBP', quality=80, method=6); print('ok')"
ls -la "C:/Users/14jak/GitHub/website/assets/projects/mega-ttt.webp"
```

Expected: file written, size between 20 KB and 60 KB. If it lands over
60 KB, retry at `quality=75`; if visibly artifacted, retry at
`quality=85`.

- [ ] **Step 2: Verify dimensions**

```bash
python -c "from PIL import Image; im = Image.open(r'C:/Users/14jak/GitHub/website/assets/projects/mega-ttt.webp'); print(im.size)"
```

Expected: `(802, 802)`. (Same square dimensions as the original PNG.)

- [ ] **Step 3: Commit**

```bash
git -C "C:/Users/14jak/GitHub/website" add assets/projects/mega-ttt.webp
git -C "C:/Users/14jak/GitHub/website" commit -m "feat: add Mega TTT gameplay screenshot from itch.io"
```

---

## Task 2: Update Mega TTT card — link, image, CTA

**Files:**
- Modify: `C:/Users/14jak/GitHub/website/index.html` (Mega TTT `<article>`)
- Modify: `C:/Users/14jak/GitHub/website/styles.css` (append one rule)

- [ ] **Step 1: Update the Mega TTT card markup**

In `index.html`, find the existing Mega TTT card. It currently looks like:

```html
            <article class="project">
              <header class="project__head">
                <span class="status status--shipped" aria-label="Status: shipped"></span>
                <span class="project__status-label">SHIPPED</span>
                <span class="project__last-commit">mar 2024</span>
              </header>
              <h3 class="project__name">Mega Tic-Tac-Toe</h3>
              <p class="project__what">Unity game on a 3&times;3 grid of linked Tic-Tac-Toe boards &mdash; each move dictates the opponent&rsquo;s next board.</p>
              <p class="project__body">Local + online multiplayer; single player vs. a Minimax-based AI with multiple difficulty levels. Self-shipped indie title.</p>
              <p class="project__metrics"><span class="metrics__prefix">//</span> unity &middot; c# &middot; minimax ai &middot; local + online mp</p>
              <p class="project__chips">unity &middot; c# &middot; minimax ai &middot; multiplayer</p>
              <pre class="project__sample" aria-label="Sample board state">+---+---+---+
| X | . | O |
+---+---+---+   active board: top-right
| . | X | . |   minimax depth 6
+---+---+---+   eval +0.21
| O | . | X |</pre>
            </article>
```

Replace it with:

```html
            <article class="project">
              <header class="project__head">
                <span class="status status--shipped" aria-label="Status: shipped"></span>
                <span class="project__status-label">SHIPPED</span>
                <span class="project__last-commit">mar 2024</span>
              </header>
              <h3 class="project__name"><a href="https://jakethehoffer.itch.io/mega-tictactoe" target="_blank" rel="noopener">Mega Tic-Tac-Toe</a></h3>
              <p class="project__what">Unity game on a 3&times;3 grid of linked Tic-Tac-Toe boards &mdash; each move dictates the opponent&rsquo;s next board.</p>
              <p class="project__body">Local + online multiplayer; single player vs. a Minimax-based AI with multiple difficulty levels. Self-shipped indie title on itch.io.</p>
              <p class="project__metrics"><span class="metrics__prefix">//</span> unity &middot; c# &middot; minimax ai &middot; local + online mp</p>
              <p class="project__chips">unity &middot; c# &middot; minimax ai &middot; multiplayer</p>
              <figure class="project__media">
                <img src="assets/projects/mega-ttt.webp" alt="Mega Tic-Tac-Toe gameplay: a 3x3 grid of sub-boards with Xs and Os, one sub-board claimed by a large blue O" loading="lazy" decoding="async" width="802" height="802" />
              </figure>
              <p class="project__cta"><a class="btn" href="https://jakethehoffer.itch.io/mega-tictactoe" target="_blank" rel="noopener">[ play on itch.io &#x2197; ]</a></p>
            </article>
```

Changes vs. the original:
- Title is now wrapped in an `<a>` to the itch.io page (same pattern as
  `trader` and `Odds Aggregator` GitHub links).
- "Self-shipped indie title." → "Self-shipped indie title on itch.io."
- The ASCII `<pre class="project__sample">` is removed entirely.
- A `<figure class="project__media">` with the new WebP screenshot is
  inserted in its place.
- A new `<p class="project__cta">` adds a clear `[ play on itch.io ↗ ]`
  button below the screenshot, styled as `.btn`.

- [ ] **Step 2: Append the new CSS rule**

In `styles.css`, find the existing `/* ---------- Project media + samples ---------- */`
block. Add this rule at the end of that block (after the `.sample__hi`
rule and before the `/* ---------- Case study ---------- */` block):

```css
.project__cta { margin: 0.875rem 0 0; }
```

- [ ] **Step 3: Verify in browser**

Serve and load the page (`python -m http.server 8000` from the repo,
then open <http://localhost:8000/#projects>). On the Mega TTT card you
should see:
- The project name "Mega Tic-Tac-Toe" rendered as an amber-on-hover link
- The gameplay screenshot taking the full card width below the chips
- A `[ play on itch.io ↗ ]` button below the screenshot

Click the title and the button — both should open the itch.io page in
a new tab.

- [ ] **Step 4: Commit**

```bash
git -C "C:/Users/14jak/GitHub/website" add index.html styles.css
git -C "C:/Users/14jak/GitHub/website" commit -m "feat: real screenshot + itch.io CTA on Mega TTT card"
```

---

## Task 3: Write the confusion-matrix render script

**Files:**
- Create: `C:/Users/14jak/GitHub/website/scripts/render-wj-cm.py`

- [ ] **Step 1: Write the script**

```python
"""Render the walking-vs-jumping confusion matrix as a PNG that matches
the v2 design tokens. One-shot; kept in the repo for reproducibility.

Usage:
    python scripts/render-wj-cm.py
Writes:
    assets/projects/walking-jumping-cm.png
"""

from pathlib import Path

import matplotlib
matplotlib.use("Agg")  # headless
import matplotlib.pyplot as plt
import numpy as np

# Numbers from the existing ASCII sample in index.html.
CM = np.array(
    [
        [182, 7],
        [5, 156],
    ]
)
LABELS = ["walk", "jump"]
ACCURACY = 0.965
F1 = 0.964

# v2 design tokens (dark theme).
BG = "#0a0e14"
FG = "#d6dee6"
DIM = "#8a96a3"
GRID = "#1c2330"
ACCENT = "#f5b342"

OUT = Path(__file__).resolve().parent.parent / "assets" / "projects" / "walking-jumping-cm.png"


def main() -> None:
    # Build a custom diverging colormap that goes from --bg-grid (off-diag)
    # to --accent (max diag). Diagonal cells will land near the high end.
    from matplotlib.colors import LinearSegmentedColormap

    cmap = LinearSegmentedColormap.from_list("v2_amber", [GRID, ACCENT])

    # We want strong colors on the diagonal and quiet ones off-diagonal,
    # so normalize by the overall max rather than per-row.
    norm_values = CM.astype(float) / CM.max()

    fig, ax = plt.subplots(figsize=(6, 4), dpi=110, facecolor=BG)
    ax.set_facecolor(BG)

    im = ax.imshow(norm_values, cmap=cmap, vmin=0, vmax=1, aspect="auto")

    # Cell text — dark on the bright diagonal, light on the muted cells.
    for r in range(CM.shape[0]):
        for c in range(CM.shape[1]):
            value = int(CM[r, c])
            color = BG if norm_values[r, c] > 0.5 else FG
            ax.text(
                c, r, str(value),
                ha="center", va="center",
                color=color,
                fontsize=18,
                fontfamily="monospace",
                fontweight="bold",
            )

    # Axes labels and ticks.
    ax.set_xticks(range(len(LABELS)))
    ax.set_yticks(range(len(LABELS)))
    ax.set_xticklabels([f"pred {l}" for l in LABELS], color=FG, fontfamily="monospace")
    ax.set_yticklabels([f"true {l}" for l in LABELS], color=FG, fontfamily="monospace")
    ax.tick_params(colors=FG, length=0)

    for spine in ax.spines.values():
        spine.set_color(GRID)

    ax.set_title(
        "confusion matrix — walking vs jumping",
        color=FG, fontfamily="monospace", fontsize=12, pad=14, loc="left",
    )
    fig.text(
        0.02, 0.02,
        f"// accuracy {ACCURACY:.3f}  ·  f1 {F1:.3f}",
        color=DIM, fontfamily="monospace", fontsize=10,
    )

    fig.tight_layout(rect=(0, 0.06, 1, 1))
    OUT.parent.mkdir(parents=True, exist_ok=True)
    fig.savefig(OUT, facecolor=BG, dpi=110)
    plt.close(fig)
    print(f"wrote {OUT}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Commit the script**

```bash
git -C "C:/Users/14jak/GitHub/website" add scripts/render-wj-cm.py
git -C "C:/Users/14jak/GitHub/website" commit -m "feat: add confusion-matrix render script"
```

---

## Task 4: Render the confusion matrix and verify

**Files:**
- Create: `C:/Users/14jak/GitHub/website/assets/projects/walking-jumping-cm.png` (via script)

- [ ] **Step 1: Run the renderer**

```bash
python "C:/Users/14jak/GitHub/website/scripts/render-wj-cm.py"
ls -la "C:/Users/14jak/GitHub/website/assets/projects/walking-jumping-cm.png"
```

Expected: prints `wrote C:\Users\14jak\GitHub\website\assets\projects\walking-jumping-cm.png`,
file size under 30 KB.

- [ ] **Step 2: Open the PNG in an image viewer**

Confirm the PNG renders with:
- Near-black background (`#0a0e14`)
- Amber diagonal cells with `182` and `156` in dark text
- Muted grey off-diagonal cells with `7` and `5` in light text
- "confusion matrix — walking vs jumping" title at top-left
- "// accuracy 0.965 · f1 0.964" subtitle at bottom-left

If colors don't pop or text is unreadable, adjust the colormap blend in
`scripts/render-wj-cm.py` and re-run.

- [ ] **Step 3: Commit the PNG**

```bash
git -C "C:/Users/14jak/GitHub/website" add assets/projects/walking-jumping-cm.png
git -C "C:/Users/14jak/GitHub/website" commit -m "feat: render walking/jumping confusion matrix in v2 palette"
```

---

## Task 5: Update Walking/Jumping card to use the new PNG

**Files:**
- Modify: `C:/Users/14jak/GitHub/website/index.html` (Walking/Jumping `<article>`)

- [ ] **Step 1: Replace the ASCII confusion matrix with a figure**

In `index.html`, find the Walking/Jumping card. It currently ends with:

```html
              <p class="project__metrics"><span class="metrics__prefix">//</span> python &middot; accelerometer &middot; signal processing</p>
              <p class="project__chips">python &middot; numpy &middot; scikit-learn &middot; signal processing</p>
              <pre class="project__sample" aria-label="Sample confusion matrix">              pred=walk  pred=jump
true=walk        <span class="sample__ok">182</span>         7
true=jump          5        <span class="sample__ok">156</span>

accuracy   <span class="sample__hi">0.965</span>   f1 0.964</pre>
            </article>
```

Replace the `<pre class="project__sample">...</pre>` block (only that
block — leave the chips and metrics lines alone) with:

```html
              <figure class="project__media">
                <img src="assets/projects/walking-jumping-cm.png" alt="Confusion matrix: 182 true walking, 156 true jumping, 7+5 misclassifications. Accuracy 0.965, F1 0.964." loading="lazy" decoding="async" width="660" height="440" />
              </figure>
```

Result: the card retains its head, name, what/body lines, metrics, and
tech-chips line, and now ends with the rendered confusion-matrix figure
instead of the ASCII block.

- [ ] **Step 2: Verify in browser**

Reload the page. The Walking/Jumping card now shows a rendered chart
instead of ASCII text. Confirm the image is visible, properly sized
inside the card, and uses the v2 dark palette so it blends with the
card surface.

- [ ] **Step 3: Commit**

```bash
git -C "C:/Users/14jak/GitHub/website" add index.html
git -C "C:/Users/14jak/GitHub/website" commit -m "feat: replace walking/jumping ASCII matrix with rendered chart"
```

---

## Task 6: Cleanup, smoke tests, push, handoff

**Files:**
- Delete: `C:/Users/14jak/GitHub/website/.tmp-mega/` (working directory)

- [ ] **Step 1: Delete the temporary download directory**

```bash
rm -rf "C:/Users/14jak/GitHub/website/.tmp-mega"
```

Confirm it's gone:

```bash
ls -la "C:/Users/14jak/GitHub/website/" | grep -i tmp || echo "clean"
```

Expected output: `clean`.

(`.tmp-mega/` was never tracked — `.tmp/` and `.tmp_*` are in
`.gitignore` — so there's nothing to `git rm`. This is just disk
hygiene.)

- [ ] **Step 2: Structural smoke tests**

Run a local server and verify everything:

```bash
cd "C:/Users/14jak/GitHub/website" && python -m http.server 8765 > "$TEMP/jh-http3.log" 2>&1 &
sleep 2
echo "=== Asset responses ==="
for path in "" styles.css script.js assets/projects/mega-ttt.webp assets/projects/walking-jumping-cm.png assets/projects/[removed].webp resume.pdf; do
  curl -s -o /dev/null -w "$path: %{http_code} (%{size_download} bytes)\n" "http://localhost:8765/$path"
done
echo ""
echo "=== Total page weight (excl. resume.pdf) ==="
python -c "
import os
paths = [
    r'C:/Users/14jak/GitHub/website/index.html',
    r'C:/Users/14jak/GitHub/website/styles.css',
    r'C:/Users/14jak/GitHub/website/script.js',
    r'C:/Users/14jak/GitHub/website/assets/projects/[removed].webp',
    r'C:/Users/14jak/GitHub/website/assets/projects/mega-ttt.webp',
    r'C:/Users/14jak/GitHub/website/assets/projects/walking-jumping-cm.png',
]
total = sum(os.path.getsize(p) for p in paths)
print(f'  TOTAL: {total:,} bytes  (target: < 200000)')
"
echo ""
echo "=== Card-level checks ==="
HTML=\$(curl -s http://localhost:8765/)
echo "Mega TTT card link to itch.io:  \$(printf '%s' \"\$HTML\" | grep -c 'jakethehoffer.itch.io/mega-tictactoe')"
echo "Mega TTT image reference:       \$(printf '%s' \"\$HTML\" | grep -c 'mega-ttt.webp')"
echo "Walking/Jumping chart ref:      \$(printf '%s' \"\$HTML\" | grep -c 'walking-jumping-cm.png')"
echo "ASCII confusion matrix gone:    \$(printf '%s' \"\$HTML\" | grep -c 'pred=walk' | sed 's/0/yes/' | sed 's/[1-9].*/no/')"
pkill -f "http.server 8765" 2>/dev/null; sleep 1
echo "server stopped"
```

Expected:
- All assets return 200
- Total page weight is around 190 KB, under the 200 KB budget
- 2 itch.io references (title link + CTA button)
- 1 mega-ttt.webp reference
- 1 walking-jumping-cm.png reference
- ASCII confusion matrix gone: `yes`

- [ ] **Step 3: Push to origin**

```bash
git -C "C:/Users/14jak/GitHub/website" push 2>&1 | tail -3
```

Expected: a clean push to `origin/main` with the 5 new commits from
Tasks 1–5.

- [ ] **Step 4: Commit the implementation plan + update .ai-sync**

The plan document itself isn't committed until execution begins. After
the smoke tests pass, commit the plan:

```bash
git -C "C:/Users/14jak/GitHub/website" add docs/superpowers/plans/2026-05-12-shipped-projects-uplift.md
git -C "C:/Users/14jak/GitHub/website" commit -m "docs: add shipped-projects-uplift implementation plan"
git -C "C:/Users/14jak/GitHub/website" push 2>&1 | tail -3
```

Then update the .ai-sync handoff:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "$HOME\.ai-sync\ai-sync.ps1" -Action handoff -Agent claude -Summary "Shipped-projects visual uplift complete. Mega TTT card now has the real itch.io gameplay screenshot plus a [play on itch.io] CTA; Walking/Jumping card now has a real matplotlib confusion matrix rendered in v2 palette. Smart Shoe card unchanged." -FilesChanged "index.html, styles.css (one new rule), assets/projects/mega-ttt.webp (NEW), assets/projects/walking-jumping-cm.png (NEW), scripts/render-wj-cm.py (NEW), docs/superpowers/specs/2026-05-12-shipped-projects-uplift.md, docs/superpowers/plans/2026-05-12-shipped-projects-uplift.md" -TestsRun "Smoke: all assets 200, page weight ~190KB (< 200KB budget), Mega TTT title links to itch.io, CTA button present, ASCII confusion matrix replaced by rendered PNG." -Blockers "none" -NextSteps "1) Visual review in real browser. 2) Consider OG image / favicon next. 3) Make repo public + enable GitHub Pages when ready to share."
```

- [ ] **Step 5: Confirm the final state**

```bash
git -C "C:/Users/14jak/GitHub/website" log --oneline | head -10
git -C "C:/Users/14jak/GitHub/website" status
```

Expected: working tree clean, recent commits include the uplift work,
`origin/main` is in sync.

---

## Closing checklist

After Task 6:
- [ ] All 6 task headings checked off above.
- [ ] `mega-ttt.webp` and `walking-jumping-cm.png` exist under `assets/projects/`.
- [ ] Mega TTT card title is now a link to the itch.io page; image renders; `[ play on itch.io ↗ ]` CTA present.
- [ ] Walking/Jumping card shows a rendered chart, not ASCII text.
- [ ] Smart Shoe card is unchanged.
- [ ] Total page weight remains under 200 KB.
- [ ] `.tmp-mega/` is gone from disk.
- [ ] All work pushed to `origin/main`.
